---
labels: ['react', 'typescript', 'book']
description: 'A Book component.'
---

import { Book } from './book';
import { mockBook } from '@learn-bit-react/data-fetching.models.book';

Book example:

A simple Book component with some text

Code Snippet:

```js
<Book book={mockBook()} />
```

Live Playground:

```js live
<Book book={mockBook()} />
```
