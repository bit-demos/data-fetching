---
description: Theme context.
labels: ['context']
---

import { ThemeContext } from './theme-context';
import { ThemeProvider } from './theme-context-provider';
import { MockComponent } from './theme-context.compositions';

A theme context.

```tsx live
() => {
  return (
    <ThemeProvider color="red">
      <MockComponent />
    </ThemeProvider>
  );
};
```
